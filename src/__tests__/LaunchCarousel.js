import { render, screen } from '@testing-library/react'
import LaunchCarousel from '../Components/LaunchCarousel'

test('it renders the LaunchCarousel component', () => {
    // const thing = screen.getByRole('checkbox', {hidden: true})
    // expect(thing).toBeInTheDocument()
})